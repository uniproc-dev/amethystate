---
title: "Field<T>"
sidebar:
  label: Field
  order: 8
---

Поле, объявленное через `#[amethystate]`, — это хендл на один путь в store.
Читают из памяти, пишут тоже сразу в память, а на диск — по дебаунсу.

<!-- shown: reading and writing a field -->
```rust
let port = state.port().get();

state.port().set(9090)?;

let raised = state.port().update(|port| port + 1)?;

state.port().modify(|port| *port += 1)?;
```
<!-- /shown -->

`get` отдаёт само значение. У поля оно есть всегда: если по пути ничего не
лежит, `get` вернёт то, что стоит в `default` у этого поля.

```rust
fn get(&self) -> T
fn try_get(&self) -> Result<T, Disagreement>
fn set(&self, value: T) -> Result<(), WriteValue>
fn update<F: FnOnce(T) -> T>(&self, f: F) -> Result<T, WriteValue>
fn modify<F: FnOnce(&mut T)>(&self, f: F) -> Result<(), WriteValue>
```

Типы в `Err` разные, и это нарочно. Запись упасть может; чтение — нет, поле
всегда что-то держит. `Err` у `try_get` — это не провал самого вопроса, а то, о
чём поле и store не договорились.

## Чтение, когда ответ может быть и не из store

`get` — чтение терпимое: значение оно отдаёт всегда и падать ему нечем.
`try_get` — то же чтение, но оно ещё и скажет, если store с ответом не
согласен.

`Err` приходит, когда пришедшее изменение не декодируется в тип поля — файл
правили мимо процесса, миграция что-то за собой оставила, кодек принял
значение, которое сам же прочитать не может, — или когда значение завернула
объявленная проверка.

Поле в это время держит последнее значение, с которым store был согласен, а
подписчиков никто не зовёт. То, что оно отдаёт, было правдой мгновение назад;
против него `default` — всего лишь догадка, сделанная на компиляции, и из
всего, чем это значение могло оказаться, она наименее вероятная. Поэтому `get`
отвечает дальше, а `try_get` — то место, где узнаёт тот, кому не всё равно.

Само обращение не падает никогда: упало раньше, а `try_get` только про это
рассказывает. И снова отвечает `Ok`, как только очередное изменение
декодируется, — то есть держится ровно столько, сколько остаётся правдой.

Отчёт отвечает на вопрос, что именно случилось, типами. `Key` есть всегда;
`Refused` появляется там, где значение завернула проверка; у закрытого store
свой `WriteError::Closed`. Как их разбирать:
[Объявление структур](/amethystate/ru/state/defining-structs/#значение-декодируется-и-оно-бессмысленно).

Значение, которое не читалось уже когда структуру открывали, — другой момент,
и решает его объявление:
[что происходит, когда значение испорчено](/amethystate/ru/state/defining-structs/#что-происходит-когда-значение-испорчено).

## Запись

`update` и `modify` — это прочитать, поменять, записать, и они **не атомарны**.
Две такие операции, столкнувшись на одном поле, потеряют один из двух
результатов — ровно как две пары `get`-потом-`set`. Где это важно, берите
запись самого store.

Разницу между ними стоит знать: `update` возвращает сохранённое значение. Оно
избавляет от `get` следующей строкой, и это то, что легло на самом деле, а не
то, что вы посчитали.

## Чего стоит запись

Диска никто не ждёт. Запись доходит до буфера, подписчики о ней слышат, а
сбрасывает дебаунсер. Поэтому поле, в которое пишут каждый кадр, стоит одной
записи в буфер за кадр и одного коммита за интервал дебаунса.

Как вместо этого дождаться диска:
[Durability](/amethystate/ru/concepts/durability/). Ожидание доводит до диска
не только это поле, а насколько больше — решает движок; там же и сказано,
какой как.

## За что запись отклоняют

Отказать могут трое: кодек работающего движка, который не умеет закодировать
это значение; путь глубже, чем берёт store; перехватчик, сказавший «нет».

Все трое отвечают тем самым `set`, который их и позвал. Кодируют значение там
же, где пишут, поэтому отказ приходит в поток управления вызывающего, а до
буфера значение не доходит вовсе.

## Куда дальше

- Как подписаться на изменения — в своём потоке или в чужом:
  [Подписки](/amethystate/ru/concepts/subscriptions/).
- Коллекции, чьи ключи появляются в рантайме:
  [ReactiveMap](/amethystate/ru/primitives/reactive-map/).
