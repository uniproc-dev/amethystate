[package]
name = "xtask"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde_json = { workspace = true, features = ["preserve_order"] }
syn = { workspace = true }
